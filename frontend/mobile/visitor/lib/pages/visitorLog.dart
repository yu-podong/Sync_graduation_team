import 'package:flutter/material.dart';

class VisitorLog extends StatelessWidget {
  const VisitorLog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('VisitorLogPage'),
      ),
    );
  }
}
